# 平台货主标准字段知识库

## 原始字段清单（你提供）

```text
1 user_id bigint 用户ID 网络身份标识信息 L2 --
2 hcb_user_id bigint hcb用户id L2 (默认) --
3 user_name string 用户姓名 网络身份标识信息 L3 --
4 birthday bigint 用户生日 个人基本资料 L3 --
5 gender int 用户性别 个人基本资料 L3 性别，男 1， 女 2，未知：-99
6 age int 用户年龄 个人基本资料 L3 --
7 mobile_mask string 手机号码_加密 L2 (默认) --
8 company_name string 注册的公司名称 L2 (默认) --
9 company_addr_mask string 注册的公司地址_加密 L2 (默认) --
10 telephone_mask string 座机电话_加密 L2 (默认) --
11 business_company_id bigint 货主所在企业id 网络身份标识信息 L2 --
12 is_business_company_user int 是否企业用户 L2 (默认) --
13 person_id bigint 自然人id 关联自然人表 L2 (默认) --
14 id_card_no_mask string 身份证号_加密 L2 (默认) --
15 is_id_card_auth_succ int 身份证号是否已经验证 L2 (默认) --
16 id_card_success_time string 身份证审核通过时间 L2 (默认) --
17 id_card_success_day int 身份证审核通过日期 L2 (默认) --
18 is_auth_success int 是否审核通过 L2 (默认) --
19 first_auth_time string 首次审核通过时间 L2 (默认) --
20 first_auth_day int 首次审核通过日期 L2 (默认) --
21 last_audit_status int 最后一次审核状态 (1：未提交 2：审核通过 `3`:第一步审核通过,第二步待审核 `4`:待审核 `5`:老司机证件待上传状态值 -`1`:审核不通过 -`2`:头像审核通过，证件审核不通过 ) L2 (默认) --
22 last_audit_time string 最后一次审核时间 L2 (默认) --
23 last_audit_day int 最后一次审核日期 L2 (默认) --
24 is_membership int 当前是否会员 L2 (默认) --
25 install_source_type int 安装来源1：前线，2：中台，3：自助注册 L2 (默认) --
26 install_sales_id string 安装销售ID L2 (默认) --
27 install_city_id int 按照货主公司地、注册地、手机号码归属地的优先级来判 L2 (默认) --
28 user_platform string 用户平台：MM/BB/MB L2 (默认) --
29 is_black_user int 是否黑名单用户 L2 (默认) --
30 create_time string 用户创建时间 L2 (默认) --
31 create_day int 用户创建日期 L2 (默认) --
32 update_time string 更新时间 L2 (默认) --
33 update_day int 更新日期 L2 (默认) --
34 first_submit_info_time string 首次提交资料时间 L2 (默认) --
35 first_submit_info_day string 首次提交资料日期 L2 (默认) --
36 company_addr string 注册的公司地址 L2 (默认) --
37 account_status bigint 账户状态-2：注销－１：拉黑 ０：未认证１：已认证 L2 (默认) --
38 bin_tags string 二进制对应的用户标签可见 http://wiki.ymmoa.com/pages/viewpage.action?pageId=18944466 L2 (默认) --
39 shipper_tag string 已不再维护，请使用tags_list字段，原注释(货主标签 `0`:无 `1`:A2 `2`:A3 `3`:A4 `99`:其他) L2 (默认) --
40 mobile string 手机号码 L2 (默认) --
41 company_name_mask string 注册的公司名称_加密 L2 (默认) --
42 id_card_no string 身份证号 L2 (默认) --
43 telephone string 座机电话 L2 (默认) --
44 install_city_name string 安装城市名称 L2 (默认) --
45 install_prov_name string 安装省份名称 L2 (默认) --
46 is_unique_mb_uid int 满帮uid是否唯一映射：1是、0否；限定=1时满帮uid一对一映射：1、一个帮帮uid对应到多个满满uid则取最新一条；2、无法映射的数据(null、空串)则为0 L2 (默认) --
47 tags_list string 用户打标列表，与ods.usercenterdb_uc_tags_info关联，关联不到的见https://wiki.amh-group.com/pages/viewpage.action?pageId=65712069 L2 (默认) --
48 original_platform int 用户来源(uc_account.platform) L2 (默认) --
49 register_flag int 是否是注册用户 0：未注册 1：已注册 L2 (默认) --
50 is_staff int 是否是内部员工 0：不是 1：是 L2 (默认) --
51 person_status int 实名认证_最新状态 L2 (默认) --
52 person_status_first_time string 实名认证_首次审核通过时间 L2 (默认) --
53 person_status_first_day int 实名认证_首次审核通过日期 L2 (默认) --
54 ss_tag int 1、用户为全新用户，历史无YMM、回头车账号；2、用户历史仅为YMM用户，无回头车账号；3、用户历史仅为回头车账号，无YMM账号；4、用户历史为回头车账号+YMM账号 L2 (默认) --
55 ss_user_id bigint 省省侧用户id L2 (默认) --
56 day int L2 (默认) --
```
